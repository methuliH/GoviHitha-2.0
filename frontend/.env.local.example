# Copy to .env.local and fill in values.

# -------------------------------------------------------------------
# Python agent backend URL
# Leave empty to use mock data (no Python backend needed).
# For local dev with a running backend: http://localhost:8000
# For production: your Cloud Run service URL (set in Chunk 13).
# -------------------------------------------------------------------
AGENT_URL=
